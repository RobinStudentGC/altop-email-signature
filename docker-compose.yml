# Docker Compose v2+ – 'version' key is deprecated/ignored, so omitted intentionally
services:
  dip-config-dash:
    build:
      context: .
      target: prod
    container_name: altop-email-signature
    ports:
      - "5194:3000"
    environment:
      - NODE_ENV=production
